package cool.furry.mc.forge.projectexpansion.gui.container.slots;

import moze_intel.projecte.gameObjs.container.inventory.TransmutationInventory;
import moze_intel.projecte.gameObjs.container.slots.transmutation.SlotInput;

public class PXInputSlot extends SlotInput {
    public PXInputSlot(TransmutationInventory inventory, int index, int x, int y) {
        super(inventory, index, x, y);
    }
}
