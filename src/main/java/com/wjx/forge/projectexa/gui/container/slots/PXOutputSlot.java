package com.wjx.forge.projectexa.gui.container.slots;

import moze_intel.projecte.gameObjs.container.inventory.TransmutationInventory;
import moze_intel.projecte.gameObjs.container.slots.transmutation.SlotOutput;

public class PXOutputSlot extends SlotOutput {
    public PXOutputSlot(TransmutationInventory inventory, int index, int x, int y) {
        super(inventory, index, x, y);
    }
}
